# TimeTracker Docker Environment Configuration
# Kopiere diese Datei zu .env und passe die Werte an

# Port auf dem die App läuft (Standard: 3000)
PORT=3000

# PocketBase API URL
# Für lokalen Server (intern, ohne Cloudflare): http://127.0.0.1:8090
# Für externen Zugriff (über Internet): https://api.nick-cloud.org
# Falls nicht gesetzt, wird https://api.nick-cloud.org als Fallback verwendet
VITE_API_URL=https://api.nick-cloud.org

# Supabase Konfiguration (optional - nur für Lovable Cloud)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_PUBLISHABLE_KEY=
# VITE_SUPABASE_PROJECT_ID=
