# TimeTracker Docker Compose Configuration
# Self-Hosted Zeit-Tracking Anwendung
#
# ============================================================================
# UI OVERHAUL v3.0 - Statistik, Kalender, Dashboard, Ziele
# ============================================================================
#
# NEUE FEATURES:
#
# 📊 STATISTIK:
#   - Reine Wochenbetrachtung (Kalenderwochen) - tägliche Vergleiche entfernt
#   - Dynamische Anzeige "Stand Dienstag" für laufende Woche
#   - Benchmark: Vergleich mit historischem 8-Wochen-Durchschnitt (gleicher Wochentag)
#   - Manueller KW-Vergleich (2 Wochen wählen + Gesamtdurchschnitt)
#   - Nur Hauptkategorien anzeigen (Unterkategorien ausgeblendet)
#   - Stacked Bar Charts + Area Charts für Visualisierung
#
# 📅 KALENDER:
#   - Visuelle Skalierung: Eintrags-Länge entspricht exakt der Dauer
#   - 16-Stunden-Raster (6:00-22:00)
#   - Formel: 1 Stunde = 1/16 der Bildschirmhöhe
#   - Vollflächige Kategorie-Farben (nicht nur Rand)
#
# ⏱️ DASHBOARD:
#   - Live-Timeline mit Pausen-Segmenten (schraffiert/semi-transparent)
#   - On-the-fly Pause-Editing: Klicken → Länge ändern → Löschen
#   - Glassmorphism-Design (backdrop-blur, transparente Karten)
#
# 🎯 ZIELE:
#   - Nur Wochenziele (tägliche Ziele komplett entfernt)
#   - Progress Circles/Rings für Fortschrittsanzeige
#
# ============================================================================
# SYNC-FIX v2.1 - Multi-Device Timer Synchronisation
# ============================================================================
# Timer-Status wird NUR aus der Datenbank geladen (localStorage ignoriert)
#
# ============================================================================
# API-Routing (dynamisch basierend auf Browser-Hostname):
# - Lokal (192.168.178.43/localhost): http://192.168.178.43:8090
# - Strato-Relay (rtracker.nick-cloud.org): https://rapi.nick-cloud.org
# - Remote (Cloudflare): https://api.nick-cloud.org
#
# Verwendung:
# 1. Kopiere .env.example zu .env und passe die Werte an
# 2. docker compose up -d --build
# 3. Öffne http://localhost:3000 im Browser
#
# Die API-URL wird automatisch basierend auf dem Browser-Hostname gewählt!
#
# Für Updates:
# docker compose down && docker compose up -d --build

services:
  timetracker:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_SUPABASE_URL=${VITE_SUPABASE_URL:-}
        - VITE_SUPABASE_PUBLISHABLE_KEY=${VITE_SUPABASE_PUBLISHABLE_KEY:-}
        - VITE_SUPABASE_PROJECT_ID=${VITE_SUPABASE_PROJECT_ID:-}
    image: timetracker:latest
    container_name: timetracker
    ports:
      - "${PORT:-3000}:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    labels:
      - "com.timetracker.description=Zeit-Tracking Anwendung"
      - "com.timetracker.version=3.0.0"

networks:
  default:
    name: timetracker-network
